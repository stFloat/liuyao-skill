---
name: liuyao-najia
description: |
  传统六爻纳甲占卜（京房八宫体系）的功能型技能：脚本先把卦算准（起卦→纳甲→六亲→六神→世应→空亡月破→变卦），再由 AI 按方法论解读。
  用于占问一时一事（财运、工作、感情、考试、出行等）。
  当用户说「六爻」「起卦」「摇卦」「测卦」「占卜」「算一卦」「X 能成吗」等时使用；
  用户给出三个数字/六爻值/要求随机起卦时也应触发。本技能只做排盘与趋势化解读，不替代现实决策。
---

# 六爻纳甲 · 算盘 + 解读

> 「卦不乱成，爻不乱发；一事一占，趋势为断。」

本技能分两层，**顺序不可乱**：

- **第一层（脚本算，确定性）**：运行 `scripts/liuyao.py`，把起卦、纳甲、六亲、六神、世应、空亡、月破、变卦全部算准，**绝不靠模型手推**。
- **第二层（AI 解读，方法论）**：拿到脚本输出的排盘后，严格按 `references/` 下的方法论断卦（以 `liuyao_method.md` 为主，辅以 `liuyao_basis.md` 理论底稿与 `liuyao_xinghui.md` 象义字典）——定用神→看旺衰→看动变生克→看世应→综合趋势化断语，每条结论标注依据。

脚本与排盘源自 baiyanwu/liuyao-skill（MIT License），断卦方法论为本技能自研（依据《增删卜易》《卜筮正宗》等公版经典）。干支与节气由 sxtwl（寿星天文历，pip install sxtwl）精确计算。

## 操作纪律（最重要）

### 第一阶段：先问清「所问何事」+ 选起卦方式（必做）
收到占卜请求后，**不要直接起卦**，先问两件事：
1. 具体问什么？（决定取哪个六亲为用神，见 `references/liuyao_method.md` 取用神表）
2. 用哪种起卦方式：
   - **A 手动掷钱**：用户自己拿三枚硬币掷六次，告诉每次几正几反 → 你换算成 6/7/8/9
   - **B 给六个数/六个爻值**：用户直接给 6 个爻值（6=老阴动、7=少阳、8=少阴、9=老阳动，初爻在前）
   - **C 随机起卦**：脚本帮你随机生成

### 第二阶段：跑脚本算盘（核心规则）
用本技能目录下的 `scripts/liuyao.py`（与本 SKILL.md 同级）计算。**永远先算后解。**

```bash
# 默认简略排盘（不保存记录）
python scripts/liuyao.py -q "所问之事" --no-save

# 完整八步推导（学习/核对用）
python scripts/liuyao.py -q "所问之事" --full --no-save

# 手动输入六爻值（初爻在前, 6/7/8/9）
python scripts/liuyao.py -q "测财运" -c 7 8 9 6 7 8 --no-save

# 指定占卦日期（影响日辰/空亡/月建/六神）
python scripts/liuyao.py -q "测事业" --date 2026-04-18 --no-save

# 可复现（指定种子，便于复现同一卦）
python scripts/liuyao.py --seed 42 -q "测试" --no-save

# 结构化输出（供进一步解析）
python scripts/liuyao.py -q "测投资" --json --no-save
```

> 路径说明：上述 `scripts/liuyao.py` 是相对本 SKILL.md 所在目录的路径。若 Agent 的工作目录不在技能目录，请使用本技能的完整绝对路径调用（例如 `D:\dwh\Code\Python\ai_test\.opencode\skills\liuyao-najia\scripts\liuyao.py`）。

脚本默认会把排盘 JSON 存到 `records/`（已被 `.gitignore` 排除）。本技能统一加 `--no-save` 避免落盘；如需留档可去掉该参数。

### 第三阶段：展示排盘 + 追问细节（必做）
1. 默认只给用户看**简略排盘**（排盘汇总表 + 世应 + 空亡月破 + 动爻 + 变卦）。
2. 排盘后**不要立即下结论**，必须追问：
   - 这件事具体是什么、目前进展到哪一步？
   - 你最想知道什么（能不能成 / 何时成 / 该怎么选）？
   - 有没有关键人物或时间节点？
3. 根据用户补充的细节，在 `references/liuyao_method.md` 的取用神表中定位正确的用神，再进入解读。

### 第四阶段：按方法论动态断卦
- 严格按 `references/liuyao_method.md` 的顺序：定用神 → 用神旺衰（月建日辰生克、空亡、月破）→ 动爻变爻生克 → 世应关系 → 六神修饰 → 综合趋势化断语。爻际作用与刑冲合害须遵循该文档第三节（含 3.1 刑冲合害铁律、归墟/云派变爻作用法）：本卦静爻一般不参与刑冲合害；刑只看象不看吉凶；冲合最常用、害少用；变爻作用默认归墟法（变爻作用于动爻，变卦静爻/本卦静爻不参与，变爻与动爻无作用时才可作用于本卦爻）。
- 每条断语**必须标注依据**（出自哪一爻的六亲/六神/生克关系），不编造、不铁口直断。
- 用趋势化表达（易/倾向/利于/需注意/待冲实），不写「必成」「必败」「大凶」。
- 用户每补充一次信息，重新聚焦对应爻位与用神，不必重起卦。

## 诚实边界
- 六爻是文化传统与概率性倾向分析，**不构成任何命运、健康、投资、职业的预言或保证**。
- 不宣扬迷信，不承诺改运、消灾、转运，不提供任何「法术」类建议。
- 凡涉及真实人生决策，请以现实情况为准、结合专业意见理性判断。
- 首次激活时**说一次**免责声明（娱乐/文化研究参考，非决策依据），后续不再重复。

## 爻值说明
| 值 | 名称 | 含义 |
|----|------|------|
| 7 | 少阳 | 静阳爻 |
| 8 | 少阴 | 静阴爻 |
| 9 | 老阳 | 阳动爻（阳→阴变） |
| 6 | 老阴 | 阴动爻（阴→阳变） |

## 数据来源与许可
- 排盘脚本 `scripts/liuyao.py`：移植自 [baiyanwu/liuyao-skill](https://github.com/baiyanwu/liuyao-skill)，采用 **MIT License**，原版权声明见 `LICENSE`。
- 纳甲规则遵循传统京房易传；八宫体系参考惠栋《易汉学》八宫卦次图。
- 断卦方法论（`references/`，以 `liuyao_method.md` 为主、配 `liuyao_basis.md` 与 `liuyao_xinghui.md`）：本技能基于《增删卜易》《卜筮正宗》等公版经典自研整理，不复制任何 NonCommercial 授权内容。
- 干支与节气由 sxtwl（寿星天文历）精确计算，月建按节气划分、日干支按天文历推算，不再使用近似基准。节气交接时刻前后按日期近似处理。
